
import React from 'react';
import { Navigate } from 'react-router-dom';

// Página de índice que redireciona para a página inicial
const Index = () => {
  return <Navigate to="/" />;
};

export default Index;
